# Release manifests hash these files byte for byte; checkouts must preserve them.
src/**/*.py -text
pyproject.toml -text
plugins/**/*.js -text
plugins/**/*.mjs -text
plugins/**/*.json -text
# Shell installers must also run in Linux checkouts made from Windows.
*.sh text eol=lf
